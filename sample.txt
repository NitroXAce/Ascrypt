

num 
    age = 23 - 69,
    olderAge = age + 69    
;

const nonNegotiable {
    str name = 'Steve \'Harvey\'';
    obj someObj {};
};
//nonNegotiable.name

obj object {
    yeet {
        str yeeted = 'I scream';
    };
};
/*
    Obj.yeet.yeeted
    returns 'I scream'
*/

str scream = 'I have a mouth and I must scream';

/* I am code block
let Names: { boyName: string; girlName: string; parentSurname: string } = { 
    boyName, 
    girlName, 
    parentSurname 
};
*/

str Names { 
    boyName;
    girlName;
    parentSurname;
};

//typeof Names str{}
//typeof Names.boyName str

//this is considered cross-contamination
//where the parent obj is a string based obj
Names {
    num age = 55;
};

obj Names {
    str boyName, girlName, parentSurname;
};

//this is acceptable to parent type object
//as objects are more forgiving to mix children types
Names {
    num age = 55;
};

Names.age;
//returns 55

type Person {
    num age = 18;
    bool isAdult() .age > 17;
    str name, lastName;
    Person extendedFamily {};
};

Person Employees {
    Steve {
        .name = 'Steve';
        .lastName = 'Harvey';
    };
    Stacy {
        .age = 22;
        .name = 'Stacy';
        .lastName = 'Studier';
    };
};

//Employees.Steve
/* Person {
    .age = 18;
    .name = 'Steve';
    .lastName = 'Harvey';
    .extendedFamily = Person{};
}*/


Person Employees [
    {
        .name = 'Steve';
        .age = 44;
        .lastName = 'Harvey';
    },
    {
        .age = 22;
        .name = 'Stacy';
        .lastName = 'Studier';
    }
];

str scream(Str saySomething){
    return saySomething ?? 'HELLO WORLD!';
};

arr listOfs [
    44, 'yeety'
];

str listOfNames [
    'Steve', 'Stacy'
];

str complex(num i){
    return if(!(i % 3)) 'is divisible by 3';
    else if(!(i % 2)) 'is divisible by 2';
    else 'this number is divisible outside of 2 and 3';
};

for(const key in Person)
    log(Person[key]);